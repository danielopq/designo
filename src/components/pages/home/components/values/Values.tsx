import './values.scss';

import { KeyValue } from '../../../../layout';

const Values: React.FC = () => {
    return (
        <section id="values">
            <KeyValue />
            <KeyValue />
            <KeyValue />
        </section>
    )
}
export default Values;